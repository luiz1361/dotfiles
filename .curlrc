# Spoof the browser identity as Google Chrome on macOS.
# Default: curl sends its own built-in user-agent like "curl/8.x.x"
user-agent = "Mozilla/5.0 (Macintosh; Intel Mac OS X 13_5_1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/118.0.5993.118 Safari/537.36"

# When following redirects, automatically send the previous URL as the Referer.
# Default: curl does NOT automatically set Referer on redirects.
referer = ";auto"

# Maximum time allowed for the connection phase (in seconds).
# Default: 0 (no limit, may hang indefinitely on slow networks)
connect-timeout = 60

# Maximum total time allowed for the entire request (in seconds).
# Default: 0 (no limit, curl waits until transfer completes)
max-time = 120

# Automatically follow HTTP redirects (equivalent to curl -L).
# Default: curl does NOT follow redirects.
location

# Request compressed content if supported (gzip/deflate/brotli).
# Default: curl does NOT request compression unless -H "Accept-Encoding" is manually set
compressed

# Fail the command on HTTP response errors (e.g., 404, 500).
# Default: curl does NOT fail; returns 0 even on HTTP errors
fail

# Display error messages even when running curl in silent mode.
# Default: curl may suppress error messages if -s or --silent is used
show-error

# Tell curl not to automatically read .netrc for credentials (but allow if specified).
# Default: curl does NOT use .netrc unless --netrc or --netrc-optional is specified
netrc-optional

# Require valid SSL by default (disallow insecure certificates).
# Default: curl verifies SSL certs; --insecure disables this
ssl-reqd

# Include HTTP response headers in the output.
# Breaks several tools depending on curl, like NeoVIM Lazy Plugins.
# Default: curl does NOT include headers unless -i or -D is specified
# include

# Show a nicer progress bar during transfers.
# Breaks several tools depending on curl, like NeoVIM Lazy Plugins.
# Default: curl uses the standard progress meter unless -s/--silent is used
# progress-bar

# Enable styled/colored output when supported by the terminal.
# Breaks several tools depending on curl, like NeoVIM Lazy Plugins.
# Default: curl outputs plain text without styling
# styled-output
