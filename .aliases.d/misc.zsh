# Fetches and displays the current weather for Dublin using the wttr.in service
weather() {
    curl -s http://wttr.in/Dublin
}

# Alias to run the spotify_player command for controlling Spotify playback
alias spt='spotify_player'
